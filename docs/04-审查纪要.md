# 初始改动审查纪要（第 4 日）

> 审查人：AI 辅助（今日工程起步）　审查时间：2026-08-22
> 范围：工作区全部未提交/新增改动 —— `docs/`（03 报告、04-CUDA 报告）、`scripts/`（collect_predictions.py 修改、evaluate.py、150M_random_test.yaml）、`README.md`、`.gitignore`

## 1. 审查方式

对每个文件执行：**通读 → 与 D3 接口约定/主路径一致性核对 → 依赖可用性核对（以能安装能运行为准）→ 敏感信息检查**。

## 2. 逐文件结果

| 文件 | 状态 | 审查结论 |
|------|------|----------|
| `docs/03-第3日实验报告.md` | 新增 | 结构对齐 6.1，正文含主路径四层与图 1/图 2；三红线无违规；与 API_CONTRACT 一致 |
| `docs/04-CUDA故障诊断报告.md` | 新增 | 环境级故障诊断（容器透传 + RmLogonRC=1），证据链完整；无代码问题、无违规；待管理员修复 GPU |
| `scripts/collect_predictions.py` | 修改 | 适配 torchvision 0.22 弃用 `output_format`：统一 `permute(0,2,3,1)` 转 HWC。与 D3 失败一例教训一致，修复正确 |
| `scripts/evaluate.py` | 新增 | CLI（--predictions/--dataset/--output/--per-video）与 API_CONTRACT 一致；过滤（no_annotation/unknown/error）+ 键盘集合精确匹配 + 鼠标皮尔逊；冒烟 1.0/1.0 已验 |
| `scripts/150M_random_test.yaml` | 新增 | 仅改动 `text_tokenizer_name: null`（纯视觉，免 gated 下载），与服务器无 HF 访问匹配 |
| `README.md` | 修改 | 新增"部署与运行/必要环境变量"：命令与脚本真实 CLI 逐一对应，含安装、启动、停止、首跑验证 |
| `.gitignore` | 新增 | 四类忽略（依赖/敏感信息/产物/实验数据），逐条带注释 |

## 3. 发现问题与处置

| 级别 | 问题 | 处置 |
|------|------|------|
| 中 | `collect_predictions.py` 的 torchvision 兼容修改未随文件留版本说明 | 已确认修改必要且正确；本次以独立提交承载，commit message 说明背景 |
| 低 | 原 README 曾写具体远端仓库地址 | 已改为"推送远端"（仓库地址仅存在于 git 配置，不入文档/报告） |
| 低 | `.vscode/` 编辑器配置此前未跟踪规范 | 已由 `.gitignore` 统一忽略 |
| 提示 | 全量评测需 GPU（待管理员修复），当前仅有冒烟结果 | 报告如实标注，不作通过判定 |

## 4. 依赖幻觉检查

- `collect_predictions.py`：依赖 torch/torchvision/官方 proto，已在服务器 uv 环境跑通 300 帧（ok=300）——**已验证可运行**
- `evaluate.py`：仅依赖 stdlib + json + 官方 proto，冒烟（假数据 1.0/1.0）通过——**已验证可运行**
- 无"看起来写了但装不了/跑不了"的依赖；未引入新第三方包

## 5. 结论

所有改动与 D3 接口约定、主路径一致；无密钥、无仓库地址、无录屏外链；依赖均已实际运行验证。**审查通过**，可进入小步提交。
